# 3D Showcase Template

A Manim starter project for `ThreeDScene`, surfaces, and camera rotation.

## Set Up

```sh
python -m venv .venv
.venv/bin/python -m pip install manim
```

On Windows PowerShell:

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install manim
```
